pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url = uri("https://api.xposed.info/") }
        google()
        mavenCentral()
    }
}

rootProject.name = "Telegram-Speed-Hook"
include ':app'
