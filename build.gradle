plugins {
    // Fleet baseline: do not downgrade below the chrootctl Android toolchain.
    id 'com.android.application' version '9.3.1' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
